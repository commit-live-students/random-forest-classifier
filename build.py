from sklearn.model_selection import GridSearchCV
from sklearn.ensemble import RandomForestClassifier


def myRandomForestClassifier(X_train, X_test, y_train, param_grid, KFold=3, seed=42, **kwargs):
    pass


def finetune_class(X_train, X_test, y_train, param_grid):
    pass